#include "letter.h"

using namespace cv;

Letter::Letter(char letterChar, int* rawArray, int height, int y_top, 
	       int y_bottom, int width, int l_sb, int r_sb) {

  // set variables
  // convert rawArray to OpenCV Mat object
  // delete original array

}
